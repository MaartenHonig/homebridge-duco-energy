{
  "pluginAlias": "DucoPlatform",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Connect your Duco ventilation system (DucoBox Energy, Focus, Silent) to Apple HomeKit via the Connectivity Board 2.0 local API.",
  "footerDisplay": "After setup, a dashboard is available at http://[homebridge-ip]:8581 showing sensor graphs and ventilation history.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Plugin Name",
        "type": "string",
        "default": "Duco Ventilation",
        "required": true
      },
      "host": {
        "title": "Duco Connectivity Board IP Address",
        "type": "string",
        "required": true,
        "placeholder": "192.168.1.100",
        "description": "The IP address of your Duco Connectivity Board on your local network."
      },
      "port": {
        "title": "Port",
        "type": "integer",
        "default": 80,
        "description": "HTTP port of the Duco Connectivity Board (default: 80)."
      },
      "pollingInterval": {
        "title": "Polling Interval (seconds)",
        "type": "integer",
        "default": 30,
        "minimum": 10,
        "maximum": 300,
        "description": "How often to poll the Duco for sensor updates. Lower = more responsive but more network traffic."
      },
      "dashboardPort": {
        "title": "Dashboard Port",
        "type": "integer",
        "default": 8581,
        "description": "Port for the sensor graphs dashboard web interface."
      },
      "boostDurationMinutes": {
        "title": "Boost Duration (minutes)",
        "type": "integer",
        "default": 45,
        "minimum": 5,
        "maximum": 240,
        "description": "How long the Boost switch keeps ventilation on medium speed (default: 45 min = 3×15 min)."
      },
      "dataRetentionDays": {
        "title": "Data Retention (days)",
        "type": "integer",
        "default": 30,
        "minimum": 1,
        "maximum": 365,
        "description": "How many days of sensor history to keep for the dashboard graphs."
      }
    }
  },
  "form": null,
  "display": null
}
